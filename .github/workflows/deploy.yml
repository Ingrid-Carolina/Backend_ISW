name: Deploy Node.js App 2 (Simple)

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            # Navigate to your app directory
            cd ~/Backend_ISW

            # Pull latest changes
            git pull origin master

            # Change to master branch
            git checkout master || echo "Fail change to master"

            # Install/update dependencies
            npm install 

            # Copy Env file into the file
            cp ../env/.env ./.env

            # # Kill existing Node.js process (if running)
            # pkill -f "node index.js" || true

            # # Wait a moment for process to fully stop
            # sleep 2

            # # Start the application in background
            # nohup node index.js > app.log 2>&1 &

            # # Wait and check if process started
            # sleep 3
            # if pgrep -f "node index.js" > /dev/null; then
            #   echo "App started successfully!"
            #   # Optional: Add health check if your app has an endpoint
            #   # curl -f http://localhost:3000/health || echo "Health check failed"
            # else
            #   echo "Failed to start app. Check app.log for errors."
            #   tail -10 app.log
            # fi
